# 更新记录

本项目所有值得玩家知道的变更都记录在此。格式参考 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.1.0/)，
版本号按语义化版本命名，**最新在最上面**。

- `### 新增` —— 新功能
- `### 变更` —— 已有行为改了（含性能与兼容性调整）
- `### 修复` —— 修掉会咬人的问题
- `### 移除` —— 删掉的东西（含配置字段）

玩家向的功能说明与已知限制见 [docs/Features.md](docs/Features.md)。

## [0.1.9] - 2026-09-23

### 新增

- **界面文案跟随游戏语言**：此前模组所有界面文字都是硬编码中文，玩家把游戏切成英文后，游戏按钮是 Resume / Settings 而模组按钮仍是「重启房间 / 换角色开新局」，中英混排。
  现在提供**中英两套文案**，按 `LocManager.Language` 选择：中文环境（`zhs` / `zht`）显示中文，其余语言显示英文。覆盖暂停菜单六个按钮（含「第 N 幕」→ `Act N`）、快捷键设置面板、四类重启与换角色 / 重新挑战的确认弹窗、刀下留人弹窗、屏幕提示、过场提示。
  切换语言后**无需重启游戏**：菜单按钮 / 快捷键面板 / 各类弹窗在下次打开时即为新语言；两个常驻提示层在下一次显示时刷新。
  > 日志里的中文**有意保留** —— 那是给排查问题用的，玩家看不到，翻译反而降低排查效率。

- **字体跟随游戏**：字体不再从系统里挑（此前写死 `SystemFont{"Microsoft YaHei UI"}`），改为问游戏自己的 `FontManager` 要当前语言该用的字体。这样中文用游戏内置中文字体、日韩用各自字体（不会缺字形），也不要求玩家机器上装有雅黑 —— 对 Steam Deck / Linux / 英文 Windows 尤其重要。
  同时挂上游戏提供的 `LocManager.SubscribeToLocaleChange`，语言切换时能收到通知。

### 修复

- **「快捷键设置」面板可能一直打不开**：面板里用 `new MegaLabel` 手工创建标题与说明，而 `MegaLabel._Ready` 里的 `MegaLabelHelper.AssertThemeFontOverride` 会在**没有 theme 字体覆盖**时直接抛异常（游戏为规避一个 Godot 退出期挂起 bug 刻意加的断言）。游戏自己的 MegaLabel 靠 `.tscn` 场景文件携带字体覆盖才通过，代码手建的没有就会抛 → 异常被上层 catch 吃掉 → 表现为"点快捷键设置没反应"。
  现在建完控件立刻补上字体覆盖。**真机验证**：修复前日志里 `快捷键设置面板已打开` 从未出现过；修复后正常出现。

## [0.1.8] - 2026-09-22

### 修复

- **跳过主菜单时仍可能硬崩（按 `F` 重启本层即进程终止）—— 上一版没修干净**：v0.1.7 只靠在资源预载与切场景之间补 2 帧把这个竞态"大概率"错开，真机日志证明兜不住 —— 崩溃点其实紧跟在 `Preloading 'characters=…'` / `Preloading 'Act=…'` 之后、**同一批续体里**就发生了，中间根本没有帧间隔，那 2 帧等于没排上。
  现在改成**确定性判据**：建 run 场景前，先用 `PackedScene.CanInstantiate()` + `SceneState.GetNodeCount()` 对关键场景（`input_settings_entry.tscn` —— 它不在任何预载集合里，正好处在"被摘掉并排上延迟释放"的名单中）做一次**不会崩**的健康校验。校验通过才继续走快速路径；不通过就判直接路径失败，自动回退到官方主菜单路径（那条路天然隔着菜单驻留帧，不会踩这个竞态）。
  取"宁可慢一次，也不要静默崩进程"：极少数情况下会多花约 1 秒走菜单中转，但不会再出现进程被 Godot 原生 `FATAL` 带走、连异常栈都留不下的情况。
  资源加载用 `CacheMode.Reuse` 而非 `Replace*` —— 只"补"不"拆"，避免为了修竞态去强拆可能仍被引用的现存实例。
  预检通过时也留一行日志（`关键场景预检通过`）：v0.1.7 的教训正是"这道防线到底有没有生效"在日志里查不到，只能靠"有没有崩"反推。
  **真机验证（2026-09-22 22:34 会话，v0.1.8）**：`重启房间` ×2、`重启本层` ×1、`重启本局` ×2、`重启新局` ×3、`换角色开新局` ×3 全部**成功完成**，且全部走**直接路径**（`回退主菜单路径` 计数为 0 = 预检从未误拦）；重启本层 637ms、重启房间 ~863ms、重启本局 ~2.5s、换角色 ~1.35s；`FATAL` / `p_index` / `cowdata` 均为 **0**，回溯之镜自身 WARN/ERROR 也是 **0**（v0.1.7 同场景下按 F 重启本层必崩）。

- **「刀下留人」每次都会打死战斗回合循环**：`CombatManager.HandlePlayerDeath` 是 **`async Task`** 方法，而拦截用的 Prefix 只返回 `bool`、**没有补 `ref Task __result`** —— 拦截时 Harmony 把返回值留成 `null`，而游戏内部的调用点是 `await CombatManager.Instance.HandlePlayerDeath(...)`（`CreatureCmd.KillWithoutCheckingWinCondition` 里），`await null` 直接抛 `NullReferenceException`，异常沿 `Kill → Damage` 一路上抛，于是**每次救人都必然打死一次回合循环**：
  ```
  [ERROR] Combat #N turn loop died while its combat is in progress;
          the combat is stuck until the room is restarted: System.NullReferenceException
  ```
  不只是日志难看：若弹窗恰好没能显示，代码会走"解除暂停继续战斗"的兜底 —— 而循环已死，战斗会**永久冻结**，正好是这功能本该消灭的那种失败。现在 Prefix 补 `ref Task __result`，拦截时返回 `Task.CompletedTask`（语义 = 死亡处理照常"做完"，只是没做事）。
  **真机证据**：
  - 修复前（2026-09-22 会话）：`刀下留人拦截 ×4` ↔ `turn loop died ×4`，完全 1:1（4 次为玩家主动测试死亡）。
  - 修复后（同日 23:14 会话）：同样死 2 次，`turn loop died` 为 **0**，两次都完整走通「复活 → 弹窗【确认】→ 解除暂停 → 回到地图回滚 → 完成」（719ms / 707ms，直接路径），回溯之镜自身 WARN/ERROR 为 **0**。

## [0.1.7] - 2026-09-22

### 新增

- **换角色开新局**：暂停菜单在"重启新局"之后多一个按钮（带确认框）。点下去 = 无痕丢弃当前局 → 回到主菜单 → **直接停在游戏自己的角色选择屏**，角色 / 进阶 / 幕数配置都在那里挑。
  - 只负责"送你到选人屏"：开局用的是游戏本体那三步（`GetSubmenuType` → `InitializeSingleplayer` → `Push`），与玩家手点「单人游戏 → 新游戏 → 选角色」同一条路径。原因 —— 换角色要连带换掉该角色专属的幕列表与进阶上限，模组自己复刻那套组装会在游戏更新后静默出错（这正是上一版刚清掉的那类脆弱代码）。
  - 无痕与四类重启同源（不写历史 / 不断连胜 / 不记成就）。
  - 代价如实写在确认框里：**当前局的存档会被删除**，之后主菜单不会有「继续游戏」把这局捞回来；比直接路径慢约 1 秒。
  - 不设快捷键（低频且不可逆）；每日挑战局与联机对局不显示该按钮。

### 变更

- **回滚骨架收口**：三条回滚流程（重启房间 / 重启本层 / 回到地图）原本各自复制了一份"无痕 + 暂停快照记录 + 直接路径失败自动回退主菜单 + 落点校验"，现合并成 `RollbackAsync` / `EnsureMapRoomAsync` 两处共用实现；五个对外入口的"模式守卫 → 重入保护 → 过场黑幕 → 兜底捕获"也收进一个 `RunGuardedAsync`。`src/Restart/` 从 1197 行降到 1059 行，最大文件 373 → 291 行。
  - 附带两处措辞与一处语义统一：每日/联机下的屏幕提示改为点名具体操作（"每日挑战局不支持重启房间"，原来统一说"重启/回滚"）；"回到地图"的落点检查失败时改为记 WARN 并继续（与"重启本层"一致），不再把已经成功的回滚判成整体失败。
- **换角色开新局**的删档前补一行日志：游戏的 `DeleteCurrentRun()` 会连 `.backup` 一起删，没有备份副本时游戏自己会记一条 `[ERROR] Error deleting path …current_run.save.backup`（只记不抛、主存档已删成）。看到它不必当故障查。

- **源码按层分目录**：`src/` 下分成 `Patches/`（挂在游戏方法上的补丁）、`Restart/`（重启与回滚流程）、`Snapshots/`（快照与持久化）、`UI/`（遮罩、提示、面板、弹窗），跨切的 `Entry.cs` / `SettingsManager.cs` / `NetGuard.cs` 留在 `src/` 根。原来 1075 行的 `RestartService.cs` 拆成六个 partial 文件（编排 / 交档 / 过场计时 / 游戏内部封装 / 重开整局 / 原地回滚）。
  纯代码搬运，不改任何签名与调用点：编译产物除 PDB 标识与编译器生成的序号外逐项一致（方法名、类型名、用户字符串集合全等）。

### 修复

- **直接路径可能让游戏硬崩（按 `F` 重启本层时进程终止）**：跳过主菜单时，我们把"资源预载收尾的 `GC.Collect` + 缓存延迟卸载"和"创建 run 场景"压进了同一批续体，于是 `NRun` 场景里游戏自己的输入设置面板会去同步加载一个刚被释放的场景资源（`SceneState` 长度 0）—— Godot 直接 `FATAL` 崩进程，日志里连异常栈都不给（只有 `Index p_index = 9 is out of bounds (size() = 0)`）。官方读档天然隔着主菜单驻留帧，所以走菜单不会遇到。现在在预载与切场景之间补 2 帧把它们隔开（多约 30ms 黑幕）。
  该隐患自 v0.1.2「跳过主菜单」起就存在，与本次重构无关；触发概率取决于当时缓存里正好卸掉了哪个资源。


- 按真机日志校正口径：重启耗时拆成三档（纯回滚 ~0.7s / 重启房间 ~0.9~1.0s / 重开整局 ~2.8~3.2s）、幕初快照大小改为实测 45~100KB、`docs/Features.md` 配置表补上有读取点却漏写的 `EnablePostCombatRetry`。

## [0.1.6] - 2026-09-19

源码全面审查后的加固版：不改正常游玩手感，只堵"静默失效"。

### 修复

- **弹窗容器被占用时"刀下留人"把战斗永久冻死**：原生弹窗容器（`NModalContainer`）在已有弹窗打开时会直接忽略新弹窗且不给任何失败信号，而此前把"已调用"当成"已显示" —— 拦截标志置位、战斗被暂停，弹窗却不出现、回调永不触发，玩家既不会死也看不到任何东西（表现为卡死）。现在会校验容器是否真的接管了这个弹窗，未接管即按"弹窗不可用"善后（复位状态 + 解除战斗暂停）。
- **弹窗未经选择就被关闭（Esc / 场景清理）时战斗永久停在暂停态**：此前只复位拦截标志、不解除暂停，而玩家已被复活 → 不再产生死亡调用 → 没有第二次机会解除。现在由弹窗的 `TreeExiting` 成对收尾。
- **确认框弹不出来时静默丢弃玩家指令**：改为当场执行并记日志（宁可少一次确认，也不要"按了键毫无反应"）。
- **快捷键误触发造成不可逆回滚**：快捷键补丁挂在 `NGame._Input`，比游戏自身的 hotkey 处理更早、且不经它的任何闸门。现在与游戏对齐补了三道守卫 —— 窗口未获焦、焦点在 `LineEdit`/`TextEdit`、有模态弹窗打开时一律不触发，并把忽略原因写进日志。
- **暂停菜单按钮克隆失败会污染游戏的「放弃游戏」**：旧逻辑在克隆失败时把重启回调连到了模板按钮（游戏的 GiveUp）上，等于点"放弃游戏"却执行重启，且每次打开菜单还会多连一条。现在跳过该按钮并告警。
- **回滚后地图上的手绘标记丢失**：复刻读档收尾时漏了 `MapDrawingsToLoad` 的回填（`GenerateMap` 会清空、只有读档流程会回填）。现已按同一步骤回填并摘走该字段。
- **跳过主菜单的快速路径与游戏存档写入并发**：游戏在切场景前会等待进行中的存档任务，直接路径跳过了主菜单也就跳过了这道等待。现在恢复场景前先等待 `SaveManager.CurrentRunSaveTask`。
- **跨局快照防护的真实漏洞**：磁盘幕初快照的跨局判据在"局标识不可用"（`RunManager._startTime` 反射失效 → 恒 0）时会被整体跳过，只剩幕号比较 —— 足以把上一局同幕号的快照当成本局幕初，回滚出另一局的卡组/金币/遗物。现在局标识不可用时直接拒绝磁盘快照（宁可只回到当前状态），并把种子作为第二道判据。
- **重启本局取不到本局种子时**：不再把空串当种子传给新局，改为明示"已改用新种子开局"。
- **日志版本号恒为 0.0.0**：产物 `AssemblyVersion` 实为 `0.0.0.0`（工程关了程序集信息生成），日志里永远看不出跑的是哪一版；改用代码内常量，与 `QuickRestart.json` 同步维护。
- **`ShouldSave` 反射失败时无痕保护静默失效**：改为在屏幕上明示"无痕保护未生效"，不再只写日志。

### 变更

- **每日挑战局只保留「刀下留人」，禁用整族"重启"**：快捷键、暂停菜单与选牌界面的按钮（不再注入）、战后"重新挑战"、执行点统一走同一道模式守卫（不再只靠"联机下不注入按钮"这一层）。原因：`DailyTime` 不在 `RunState` 里，重开一局必然得到"模式 Daily 但无每日结算依据"的混合态；"重启新局"更是直接重 roll 每日种子。
  每日局的致命死亡仍然弹窗救场，两个出路按模式给语义：【确认】回到地图 = 原地回滚（放行）；【取消】放弃本局 = 调游戏官方 `RunManager.Abandon()` 正常结算，并新增一次性"死亡放行闩"（官方放弃会再强制杀一次玩家，不放行会被自己的拦截器反复弹窗、永远结算不完；该闩按局标识记录，开新局自动失效）。
- **重启本层也在回滚期间暂停快照记录**（与重启房间 / 回到地图一致），不再把正确性押在"同局同幕"字段相等上。
- **无痕保护的说法按实现收窄**：回滚走游戏官方读档流程，而该流程自带"读档次数 +1 并写一次存档"（`SaveManager.IncrementNumReloads`，不受存档开关约束），所以战绩里的**读档次数**会计入每次回滚、结算时随指标上传；历史 / 连胜 / 成就三项不受影响。
- **显示名统一为「回溯之镜」**（与游戏内一直显示的名字一致；此前发布包与文档文件名写作"境"）。MOD 目录与 DLL 仍为英文 `QuickRestart`。
- 文档与诊断对齐实现：重启耗时按实测分两档写清（回滚 ≈0.65~0.70s / 重开整局 ≈2.5s）、补上"刀下留人只在战斗中生效"的范围说明、"补记选路前快照"的日志不再把新局误写成"读档继续游戏"。

### 移除

- 每次进房多做的一次整局序列化（`EnterRoom` 前缀快照 `PreRoomSnapshot` 在改用"掷骰前快照"后已无任何读取点）连同相关死字段一并删除。
- 两个从未生效的配置字段：`RetryCombatKey`（没有任何按键读取点）、`EnableWinStreakProtection`（预留无实现）。旧配置文件里残留的值会被自动忽略。

### 仓库

- 补 MIT `LICENSE` 与 `.gitattributes`（源码统一 LF，避免别人 clone 后一行改动变成整文件 diff）。
- `CopyMod` 构建目标补 `CopyModOnBuild` 开关：`dotnet build -c Release -p:CopyModOnBuild=false` 可只验编译、不碰 `mods/`。
- 构建文档补上：不要用 `-t:Compile`、游戏更新后需把 `RitsuLibDir` 指到新的 `compat/<版本>` 目录、以及 Publicizer 只改编译期可见性所以私有成员必须走反射的约定。

## [0.1.5] - 2026-09-15

### 变更

- **联机对局中本模组完全不生效**。联机对局由输入同步与校验和保护，单方面本地回滚会被判定为状态不一致，因此不适合在联机中使用。现在联机下：快捷键不执行（屏幕提示"联机模式下不可用"）、暂停菜单与战斗结算界面不显示本模组按钮、生命归零不拦截（交给游戏官方流程）。单人模式完全不受影响。

### 修复

- 刀下留人的死亡判断由"玩家列表第一位"改为**本地玩家**（此前在联机对局中，队友死亡会误触发"刀下留人"弹窗）。
- 打开开发控制台（dev console）时按重启快捷键会误触发本模组功能；现在控制台打开期间快捷键不响应（与游戏自身的输入处理保持一致）。

## [0.1.4] - 2026-09-15

### 新增

- **读档继续游戏也能真正回到「本幕起点」**。游戏存档只保存"当前状态"、不含本幕开场数据，此前用「继续游戏」读档后按 `F` 只能回到读档那一刻（感受是"按了没动"）。现在每次真实进入新幕时，幕初完整快照会自动写入磁盘（`%APPDATA%\SlayTheSpire2\mod_data\QuickRestart\act_start_snapshot.json`，约 50KB，仅保留最新一份）；之后无论读档继续还是大退重开，按 `F` 都能完整回滚到该幕起点（金币 / 卡牌 / 遗物 / 药水 / 血量与地图进度）。
  若本幕开始时 mod 尚未运行（例如首次安装时已处于幕中），磁盘上没有该幕数据，此时仍退回读档点，过场黑幕会明确写出原因。

### 修复

- **一处运行时异常导致四类重启集体退化**：现象是重启房间变成"牌序重随机的重开"、重启本层变成"仅重进本幕"、刀下留人有时原地重开战斗。原因是用于识别"是否同一局"的局标识（非公开字段，编译器放行但运行环境拒绝直读）取不到，导致所有快照归属校验失败、全部走兜底路径。改用标准反射读取，并新增降级链（局标识不可用 → 种子比对 → 放行并记日志），单一环节异常不再让功能整体失效。
- **重启本层落点兜底**：回滚后若不是地图屏会自动纠正，并在日志记录落点（此前"按了没回地图"无法定位）。

### 变更

- 过场黑幕上直接显示快照来源与回滚目标（如读档点场景），无需翻日志。
- 同一局同一幕只尝试一次磁盘读取，避免反复解析快照文件。

## [0.1.3] - 2026-09-15

### 变更

- **重启房间免确认**：按 `R` 或点击暂停菜单「重启房间」直接执行，不再弹防误触确认框（反复练习同一场战斗时不必每次点确认，可连按）。防误触由 0.5 秒触发冷却承担；重启本层 / 本局 / 新局仍保留确认框，战后「重新挑战」仍弹确认框。`ShowConfirmationDialog` 语义不变，仍控制其余操作的确认框。

### 修复

- **刀下留人后地图"前进一层"**：触发刀下留人并选择「回到地图重新挑战」后，地图进度/位置凭空前进一格（重启房间也有同类隐患）。原因是回滚走了官方「重进最后访问节点」的读档路径（`LoadIntoLatestMapCoord`），它会按节点类型重掷/新建房间并往地图点历史追加快照外的格子。改为**精确落点** —— 「回到地图」直接停在地图屏；「重启房间」按原坐标重新进入本节点（等价于第一次点击该节点）；「重启本层」落回本幕起点地图屏。三者都不再改动地图历史。
- **问号房（?）重启后内容变化**：在问号节点进入的随机房里「重启房间」，内容会变成另一批。原因是问号节点的房间类型由掷骰决定（消耗 RNG），而旧快照捕获于掷骰之后，重新进入时会用已推进的 RNG 再掷一次。改用**掷骰之前**的快照回滚，重新进入本节点时重演同一次掷骰与遭遇抽取，问号房内容与首次完全一致。

## [0.1.2] - 2026-09-15

### 新增

- **过场黑幕**：重启期间平滑淡入淡出，不再闪现主菜单画面。
- 配置项 `FastRestartSkipMenu`（默认 `true`）：重启时跳过主菜单中转、显著缩短黑屏；如遇异常可改为 `false` 回退旧流程。

### 变更

- **重启提速**：四类重启与「回到地图」全部改为直接回滚，省掉「回主菜单 → 再进游戏」的两次场景切换与主菜单资源预载。实测（同角色同房间）黑屏总时长从约 2.7s 降到约 1s（重启本层 ~0.9s、重启房间 ~1.1s、重启本局 ~1.9s，其中大部分是游戏重建场景的固有耗时）。
- 兼容 STS2-RitsuLib **0.6.x**（框架大改版）与 0.5.x。升级 0.6.0 请安装官方完整包（含 `shared/`、`compat/`、`assets.zip`），不能只复制根 DLL。

### 修复

- **「重新挑战」按钮点不动**：按钮的 `ProcessMode` 被误设为 `Disabled`（收不到任何输入），改为 `Always`。
- **「重启本层」可能回到上一局**：用游戏自带流程开新局（如上局死于第 1 幕）时，本层快照会残留旧局数据并覆盖新局；现按种子校验快照归属，不属于当前局的一律走安全回退。
- **重启进行中重复触发**：新增重入保护，一次重启未结束时再按快捷键会被忽略并记日志，不再出现两条流程交错导致的黑屏。
- **刀下留人弹窗创建失败会永久冻结战斗**：失败时自动解除暂停、放行战斗（再次死亡会重新拦截）。
- **直接路径的稳定性兜底**：任何一步异常自动回退旧流程（日志可见 `直接路径失败…回退主菜单路径`），不会卡死。

## [0.1.1] - 2026-09-15

### 修复

- **重启房间后商店物品会"刷新"**：在商店里按 `R` 重启房间后，货架上的卡牌 / 遗物 / 药水变成另一批。原因是重启时房间被**进入了两次** —— 官方读档流程本身就会按存档中的地图坐标自动送回本房间（这一次随机数与首次进房完全相同，是正确结果），而 mod 随后又手动进了一次房，商店被第二次生成，此时生成用的随机数已被第一次消耗，于是货架"凭空刷新"。现在读档已自动重进本房间时不再重复进房，重启后商店库存与首次进店完全一致。
- 同源问题一并修复：战斗房、事件房、宝箱房同样不再"生成两次"，重开后的怪物编成、事件内容、宝箱遗物与首次进房一致 —— "精确重现"承诺现在覆盖全部房间类型。

### 变更

- **刀下留人改为「回到地图重新挑战」**：死血弹窗的确认按钮不再原地重开战斗，而是回到进入本房间之前（选路前）的地图 —— 血量 / 牌组 / 金币 / 药水全部回退，连地图上的访问记录一并回退（相当于"这一格还没走过"），可以重新选路，也可以重新进入刚才那个节点再战。

## [0.1.0] - 2026-09-14

### 新增

首个发布版本：

- **四类重启** —— 重启房间 / 重启本层 / 重启本局 / 重启新局，全部基于完整运行快照回滚，RNG 精确重现。
- **刀下留人** —— 生命归零时冻结战斗，可选择「重新挑战当前战斗」或「放弃本局」。
- **战后重新挑战** —— 战斗结算的选牌界面一键放弃奖励、整场重打。
- **快捷键** —— 四项操作可在暂停菜单内自定义按键，战斗中直接可用。
- **无痕保护** —— 重启不记失败、不断连胜、不写历史与成就。
